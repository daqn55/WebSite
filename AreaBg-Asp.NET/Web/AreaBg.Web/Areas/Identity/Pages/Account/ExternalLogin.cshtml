@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Създаване на профил с Facebook акаунт";
}

<h4 class="font-sp">@ViewData["Title"]</h4>
<hr />

<p class="text-info font-sp">
    Успешно се регистрирахте с <strong>@Model.LoginProvider</strong> акаунт.
    Моля въведете имейл и натиснете на бутона Регистрация за да завършите вписването.
</p>

<div class="row">
    <div class="col-5 font-sp">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-purple-btn">Регистрация</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
